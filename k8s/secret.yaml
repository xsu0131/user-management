apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: production
type: Opaque
stringData:
  db-password: "admin"
  jwt-secret: "replace_with_real_jwt_secret"
  aws-access-key: "replace_with_real_access_key"
  aws-secret-key: "replace_with_real_secret_key"
